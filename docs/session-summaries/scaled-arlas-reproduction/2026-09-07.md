# Scaled ARLAS reproduction — 2026-09-07

- Repository: `adverserial_agent`
- Timezone: `America/Los_Angeles` (`PDT`)
- Overall status: ready for next phase

## Session 10:32

### Objective

Prepare a scientifically defensible, single-GPU ARLAS-style reproduction that
can later support the CoTA-Break capability experiments and transfer its working
context between computers and coding agents.

### Current state

The repository contains a scaled-reproduction protocol, a conservative 24 GB
GPU configuration, and a dependency-free deterministic security environment.
The environment records benign utility and attack success independently. The
next experimental work must run on a different computer with a 24 GB NVIDIA GPU;
the computer used for this session is an Apple M2 Pro without CUDA.

### Decisions and rationale

- Describe the work as an `ARLAS-style scaled reproduction`, not an exact
  reproduction, because the local design uses smaller models and fewer episodes.
- Start with `Qwen/Qwen3-1.7B`; test 4B only after measuring competence and VRAM.
- Use 4-bit NF4 QLoRA, a 2,048-token initial context, GRPO group size 2, and
  separate rollout/training/evaluation phases to fit the 24 GB envelope.
- Require deterministic replay and independent utility/security verification
  before installing the full RL stack or spending GPU time.
- Use development, validation, and sealed pilot-test partitions.

### Work completed

- Added the project overview in `README.md`.
- Added the dated pilot protocol in `docs/research-protocol-v0.1.md`.
- Added the initial compute profile in `configs/single_gpu_24gb.json`.
- Added the deterministic environment in `src/cotabreak/toy_env.py`.
- Added verifier and replay coverage in `tests/test_toy_env.py`.
- Added a repository-portable session-summary skill at
  `skills/session-summary/SKILL.md` and repository-wide guidance in `AGENTS.md`.

### Verification

- `python -m unittest discover -s tests -v`: five tests passed.
- The session-summary skill passed the bundled skill structure validator.
- Hardware probe on the current computer found Apple M2 Pro graphics and no
  CUDA or `nvidia-smi`.
- Version-control state at the checkpoint contains uncommitted additions for
  `AGENTS.md`, `skills/`, and this session-summary material.

### Open issues or blockers

- The 24 GB NVIDIA GPU is on a different computer, so CUDA and model feasibility
  have not yet been measured.
- The AgentDojo pilot suite has not been selected.
- Model and RL dependencies have not been installed or pinned.
- No model weights have been downloaded.

### Next actions

1. On the NVIDIA machine, record GPU model, driver, CUDA, Python, system memory,
   and free disk space.
2. Confirm that the repository includes this summary and the scaled-reproduction
   files.
3. Create a clean environment and pin a compatible Transformers, PEFT, TRL,
   bitsandbytes, Accelerate, and AgentDojo stack using current documentation.
4. Run one 4-bit QLoRA forward/backward/optimizer step with
   `Qwen/Qwen3-1.7B` and record peak VRAM.
5. Evaluate 20–30 benign episodes across candidate AgentDojo suites before
   selecting the pilot domain.

### Resume prompt

> Read `AGENTS.md` and `docs/session-summaries/INDEX.md`, then continue the
> scaled ARLAS reproduction from the latest summary. Start with the NVIDIA
> hardware profile and Qwen3-1.7B model-feasibility probe. Do not begin GRPO
> until deterministic replay, verifier separation, and benign competence gates
> pass.

## Session 11:42

### Objective

Separate the reusable session-summary workflow from the repository-specific
summary records.

### Current state

The `session-summary` skill is installed globally at
`~/.codex/skills/session-summary/SKILL.md`. This repository retains only its own
summary index and dated, topic-specific summaries under
`docs/session-summaries/`.

### Decisions and rationale

- Reusable skill instructions belong in the global Codex skill directory.
- Project state belongs in the relevant repository so it travels with the code.
- Paths written inside summaries remain repository-relative for portability
  across computers and coding agents.

### Work completed

- Installed and validated the global `session-summary` skill.
- Removed the duplicate repository-local skill.
- Updated `AGENTS.md` to invoke the global skill while requiring generated
  summaries to remain repository-specific.

### Verification

- The skill creator's validator reports `Skill is valid!` for the global path.
- Confirmed that `skills/session-summary/SKILL.md` no longer exists in this
  repository.

### Open issues or blockers

- Other coding-agent installations must have an equivalent global skill or use
  the summary format already stored in this repository.

### Next actions

1. Continue writing future summaries under the matching repository's
   `docs/session-summaries/<topic>/YYYY-MM-DD.md` path.
2. Resume the scaled ARLAS reproduction on the 24 GB NVIDIA machine.

### Resume prompt

> Use the global `session-summary` skill for handoffs, but keep every generated
> summary in this repository under `docs/session-summaries/`. Resume the scaled
> ARLAS reproduction from the latest indexed topic summary.
